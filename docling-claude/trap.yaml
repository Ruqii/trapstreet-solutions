tasks:
  tenancy-agreement:
    description: Docling parser → Claude reasoning
    cmd: uv run python solution.py
    traptask: ../../trapstreet-tasks/tasks/pdf_reader/tenancy_agreement
    timeout: 300             # first case downloads OCR models; later cases use cache
    file_outputs:
      - usage.json
