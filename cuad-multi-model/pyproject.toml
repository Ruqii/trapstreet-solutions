[project]
name = "cuad-multi-model-solution"
version = "0.1.0"
description = "Routes the CUAD clause-extraction task to any Claude model or OpenRouter model via the MODEL env var"
requires-python = ">=3.14"
dependencies = [
    "anthropic>=0.40.0",
    "openai>=1.50.0",
]
