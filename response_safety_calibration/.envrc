# Keys from ./.env (gitignored). tp's cost proxy only meters a vendor whose key is
# in the shell that runs `tp run`, so keys load here, never inside solution.py.
dotenv_if_exists
# The task's grading pack is bound on UAT.
export TRAPSTREET_URL=https://uat.trapstreet.run
