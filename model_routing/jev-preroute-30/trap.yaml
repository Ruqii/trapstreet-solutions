name: jev-preroute-30
cmd: uv run ../solution.py --arm jev-preroute-30
profile:
  model:
    - claude-haiku-4-5
    - claude-opus-5
    - jev-1.13.0
  framework: jev-preroute-30
  notes: >-
    Before any answer, Jev (jev-1.13.0) judges whether a cheap model would get the question wrong; at or above a threshold that escalated 28% of calibration questions Opus answers, otherwise Haiku. No extended thinking; no refusal fallback.
timeout: 900
tasks:
  model-routing:
    source: git+https://github.com/trapstreet/trapstreet-tasks@7f36c68b831fbc1454961ceab9bec4f2313bff22#subdirectory=tasks/model_routing
