name: haiku-only
cmd: uv run ../solution.py --arm haiku-only
profile:
  model:
    - claude-haiku-4-5
  framework: haiku-only
  notes: >-
    Claude Haiku 4.5 answers every question. The cheap bound. No extended thinking; no refusal fallback.
timeout: 300
tasks:
  model-routing:
    source: git+https://github.com/trapstreet/trapstreet-tasks@ffecb9fbdbaeb2e41acdf5b6a3767764b6a237d5#subdirectory=tasks/model_routing
