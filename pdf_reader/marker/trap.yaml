tasks:
  test:
    description: Marker parser → Claude reasoning
    cmd: uv run python solution.py
    traptask: ../../trapstreet-tasks/tasks/pdf_reader/tenancy_agreement
    timeout: 600             # first case downloads ~2GB of Marker model weights
